# Dependencies
node_modules/

# Capacitor build output (generated by build.cjs + cap sync — not source)
www/

# Android: do NOT ignore the whole folder — the hand-written native source
# (PacerTimerService.java, PacerTimerPlugin.java, MainActivity.java,
# AndroidManifest.xml, custom res/ icons, gradle config) must be tracked.
# The project's own android/.gitignore already excludes the generated bulk:
# build artifacts, copied web assets, local.properties, APKs, generated config.

# macOS
.DS_Store