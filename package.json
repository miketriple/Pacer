{
  "name": "pacer",
  "version": "1.0.0",
  "private": true,
  "description": "Guided rhythm for any workflow",
  "scripts": {
    "build":     "node build.js",
    "cap:sync":  "node build.js && cap sync android",
    "cap:run":   "node build.js && cap run android",
    "cap:open":  "cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capacitor/android": "^8.0.0"
  }
}
